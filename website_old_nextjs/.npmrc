# Disable workspace detection for Vercel builds
# This ensures pnpm installs only website dependencies
# and doesn't try to use the parent monorepo workspace
link-workspace-packages=false
shamefully-hoist=true

