{
  "name": "aggregation-worker-service",
  "version": "1.0.0",
  "description": "External worker service for processing aggregation jobs",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "ts-node src/index.ts",
    "docker:build": "docker build -t aggregation-worker .",
    "docker:run": "docker run aggregation-worker"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "bullmq": "^5.12.6",
    "dotenv": "^16.4.5",
    "ioredis": "^5.4.1"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "prisma": "^5.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
